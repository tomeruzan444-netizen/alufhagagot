# אלוף הגגות - static site
# Works on Apache and on LiteSpeed (Hostinger), which reads .htaccess natively.

Options -Indexes
DirectoryIndex index.html

AddDefaultCharset UTF-8
AddType text/html .html

<IfModule mod_rewrite.c>
  RewriteEngine On

  # ---- 1. force HTTPS -------------------------------------------------------
  RewriteCond %{HTTPS} !=on
  RewriteCond %{HTTP:X-Forwarded-Proto} !https
  RewriteRule ^(.*)$ https://%{HTTP_HOST}/$1 [R=301,L,NE]

  # ---- 2. www -> non-www (what the WordPress site did) ----------------------
  RewriteCond %{HTTP_HOST} ^www\.(.+)$ [NC]
  RewriteRule ^(.*)$ https://%1/$1 [R=301,L,NE]

  # ---- 3. duplicate pages -> the original, older URLs -----------------------
  # Matched on %{REQUEST_URI} because that keeps the percent-encoded Hebrew;
  # RewriteRule alone sees the decoded path. NE stops Apache re-escaping the %.
  # /איטום-גגות-בגן-יבנה-מידע-רלוונטי-ומחיר/
  RewriteCond %{REQUEST_URI} ^/%D7%90%D7%99%D7%98%D7%95%D7%9D-%D7%92%D7%92%D7%95%D7%AA-%D7%91%D7%92%D7%9F-%D7%99%D7%91%D7%A0%D7%94-%D7%9E%D7%99%D7%93%D7%A2-%D7%A8%D7%9C%D7%95%D7%95%D7%A0%D7%98%D7%99-%D7%95%D7%9E%D7%97%D7%99%D7%A8/?$ [NC,OR]
  RewriteCond %{REQUEST_URI} ^/איטום-גגות-בגן-יבנה-מידע-רלוונטי-ומחיר/?$ [NC]
  RewriteRule ^ /איטום-גגות-בגן-יבנה/ [R=301,L]

  # /חומרי-איטום-שונים-אז-אילו-חומרים-קיימי/
  RewriteCond %{REQUEST_URI} ^/%D7%97%D7%95%D7%9E%D7%A8%D7%99-%D7%90%D7%99%D7%98%D7%95%D7%9D-%D7%A9%D7%95%D7%A0%D7%99%D7%9D-%D7%90%D7%96-%D7%90%D7%99%D7%9C%D7%95-%D7%97%D7%95%D7%9E%D7%A8%D7%99%D7%9D-%D7%A7%D7%99%D7%99%D7%9E%D7%99/?$ [NC,OR]
  RewriteCond %{REQUEST_URI} ^/חומרי-איטום-שונים-אז-אילו-חומרים-קיימי/?$ [NC]
  RewriteRule ^ /חומרי-איטום/ [R=301,L]

  # ---- 4. WordPress URLs that answered 200 but have no static equivalent ----
  # Verified against the live WordPress site before the cutover, so none of
  # these turns into a 404 the day the domain moves.

  # Rank Math sitemaps Google already knows about -> the one we publish
  RewriteRule ^sitemap_index\.xml$ /sitemap.xml [R=301,L]
  RewriteRule ^(post|page|category|author|product)-sitemap\.xml$ /sitemap.xml [R=301,L]
  RewriteRule ^main-sitemap\.xsl$ /sitemap.xml [R=301,L]
  RewriteRule ^wp-sitemap.*\.xml$ /sitemap.xml [R=301,L]

  # blog pagination - WordPress served these with a canonical to the parent,
  # so sending them there keeps the same signal
  RewriteRule ^page/[0-9]+/?$ / [R=301,L]
  RewriteRule ^(.+)/page/[0-9]+/?$ /$1/ [R=301,L]

  # per-page RSS feeds
  RewriteRule ^(feed|comments/feed)/?$ / [R=301,L]
  RewriteRule ^(.+)/feed/?$ /$1/ [R=301,L]

  # admin, login and the XML-RPC endpoint
  RewriteRule ^wp-admin/?$ / [R=301,L]
  RewriteRule ^wp-login\.php$ / [R=301,L]
  RewriteRule ^xmlrpc\.php$ - [G,L]
  RewriteRule ^wp-json(/.*)?$ - [G,L]
</IfModule>

ErrorDocument 404 /404.html

<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/css text/plain text/xml application/xml application/javascript image/svg+xml application/json
</IfModule>

<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType text/css "access plus 1 year"
  ExpiresByType application/javascript "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/webp "access plus 1 year"
  ExpiresByType video/mp4 "access plus 1 year"
  ExpiresByType font/woff2 "access plus 1 year"
  ExpiresByType text/html "access plus 0 seconds"
</IfModule>

# Staging / preview hosts must never enter the index - otherwise the temporary
# Hostinger domain competes with roofschamp.co.il for the same content.
<IfModule mod_setenvif.c>
  SetEnvIf Host "hostingersite\.com$" IS_STAGING
  SetEnvIf Host "^\d+\.\d+\.\d+\.\d+$" IS_STAGING
</IfModule>
<IfModule mod_headers.c>
  Header set X-Robots-Tag "noindex, nofollow" env=IS_STAGING
</IfModule>

<IfModule mod_headers.c>
  Header set X-Content-Type-Options "nosniff"
  Header set Referrer-Policy "strict-origin-when-cross-origin"
  Header set X-Frame-Options "SAMEORIGIN"
  Header set Permissions-Policy "geolocation=(), microphone=(), camera=()"
  # Assets carry a ?v=<content hash>, so a year-long immutable cache is safe:
  # a change produces a new URL rather than a stale file.
  <FilesMatch "\.(css|js|png|jpe?g|webp|woff2|mp4)$">
    Header set Cache-Control "public, max-age=31536000, immutable"
  </FilesMatch>
  # HTML must revalidate every time, or a visitor keeps pointing at the old
  # asset URLs and never sees a redesign.
  <FilesMatch "\.html$">
    Header set Cache-Control "public, max-age=0, must-revalidate"
  </FilesMatch>
</IfModule>

# The deploy folder should only ever contain the site, but be explicit.
<FilesMatch "^(package(-lock)?\.json|README\.md|\.gitignore|.*\.tpl)$">
  Require all denied
</FilesMatch>
